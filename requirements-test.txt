# Test-only dependencies (MLX-47 contract/smoke suites).
# Install: pip install -r requirements-test.txt
# CVE-2025-71176: pytest<=9.0.2 uses predictable /tmp/pytest-of-* dirs on UNIX.
pytest>=9.0.3
pytest-django>=4.8
pytest-mock>=3.14
pytest-cov>=5.0
jsonschema>=4.23
